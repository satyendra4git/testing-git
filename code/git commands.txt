Git commands:-

git status

git add .

git commit -m "your message - what you have done"

git pull origin main

(Esc + :(colon)+x then enter)

git push origin branchname

----------------------------------------------

git config --global user.email "satyendra@ogwebsolutions.com"
git config --global user.name "satyendra kumar"
